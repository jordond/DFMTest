<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_audio"
    app:startDestination="@id/audioListFragment"
    >

<!-- This will be RED in Android Studio, but should still compile just fine :)   -->
    <fragment
        android:id="@+id/audioListFragment"
        android:name="com.worldturtlemedia.dfmtest.audiofull.AudioListFragment"
        android:label="Audio Picker"
        tools:layout="@layout/audio_list_fragment"
        />

</navigation>